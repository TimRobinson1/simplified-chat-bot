<h1>Add a Chatbot Sentence</h1>
<%= form_for @statement, html: { multipart: true } do |form| %>
  <%= form.label :message %>

  <%= form.text_area :message, id: 'chatbot-statement-input' %>

  <div class='submit-field'>
    <%= form.submit %>
  </div>
<% end %>
